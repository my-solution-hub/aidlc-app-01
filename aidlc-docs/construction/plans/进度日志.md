# Progress Log

## Phase 0 — 规划 [COMPLETE]
- 勘察 baseline（全 Test 桩）、提取 intermediate + 前端契约
- 策略：移植 intermediate（前后端一致的官方答案）+ 补 Android 缺口端点
- 创建 后端实现计划.md / ../../inception/reverse-engineering/服务功能清单.md / 进度日志.md

## Phase 1 — Auth Service [COMPLETE]
- 后端：移植 27 个业务文件（UserEntity/登录登出/用户列表/JWT/Redis缓存/V2建表）
- 编译：`mvn -o compile` → **BUILD SUCCESS**（Java 21.0.11）
- 前端：移植 24 个文件（services/api、types、4管理页、登录接真实API），删除 MOCK_USERS
- 前端构建：`npm run build` → **✓ built**（tsc + vite，732 模块）
- codex 验证：API 映射匹配、分层链路 coherent ✅
  - ⚠️ 已知 gap：logout 写入 Redis 黑名单但 isBlacklisted() 未在校验链路调用（与 intermediate 参考一致；token 校验在 Gateway 侧）→ 留待 Phase 6 端到端处理

## Phase 2 — Points Service [COMPLETE]
- 移植 14 文件（PointRuleEntity/规则分页/V2建表），mvn BUILD SUCCESS
- 前端 PointRules 页已在 Phase1 批量移植

## Phase 3 — Product Service [COMPLETE]
- 移植 30 文件（Product/Category Entity、CRUD、类目树、库存悲观锁），mvn BUILD SUCCESS
- 前端 Products/CreateProduct/Categories 已移植

## Phase 4 — Order Service [COMPLETE]
- 移植 19 文件（ExchangeRecord、记录/详情/统计），mvn BUILD SUCCESS
- 前端 ExchangeRecords 已移植

## Phase 5 — Android 对齐 [COMPLETE（契约层）]
- 重写 ApiService.kt：全部改为 POST + 网关前缀(auth/product/point/order)/api/v1 + Result<T> 信封
- baseURL 改 http://10.0.2.2:8080/（去掉 /api/，路径已含前缀）
- PageResultDto 对齐后端：current/size/total/pages/records
- ListProductRequest 对齐后端：page/size/name/category
- 重写 ShopRepository：DTO→领域模型映射，11 个页面签名不变
- AuthRepository：解 Result 信封 + 存 userId
- codex 验证：发现 2 处契约不一致(records vs list、page 字段) → 已修复
- ⚠️ 约束：本机无 Android SDK，无法 gradle 编译验证(只校验语法/契约)
- ⚠️ 缺口：Android 员工流程端点(point/balance、order/exchange、product/get)
  intermediate 后端未提供 → 已在 Android 侧定义契约，后端待补(见下)

## Phase 6 — 员工端后端补全 + 端到端一致性 [COMPLETE]
- Product：新增 product/get（详情）+ internal/deduct-stock + internal/restore-stock，编译✅
- Points：新增账户领域(account)：balance/transaction/list + internal/point/adjust(INIT/DEDUCT/ADD)
  + 账户/流水表 V3 + PointErrorCode，编译✅
- Order：员工兑换 Saga（子代理实现 + 我独立复检）：
  - public/order/exchange（兑换下单，跨服务调 product 扣库存 + points 扣积分 + Saga 补偿）
  - public/order/list（员工订单分页）+ public/order/get（详情）
  - V3 加字段(product_id/user_id/quantity/image)，编译✅
- Android：CreateExchangeRequest/ExchangeRecordDto 对齐真实兑换响应
- codex 验证 Saga：happy path + 基础补偿正确；2 个高级分布式问题记为已知限制
  （补偿失败需重试队列、网络超时需幂等键——workshop demo 非阻塞）

## 最终全量复检 [COMPLETE]
- 4 后端 mvn compile 全部 BUILD SUCCESS
- 前端 npm build ✅ built
- 全部端点前后端契约对齐（Web + Android 同一网关契约）

## 测试结果
| Phase | 后端编译 | 前端构建 | codex |
|---|---|---|---|
| 1 auth | ✅ SUCCESS | ✅ built | ✅ 通过(1 known gap) |
