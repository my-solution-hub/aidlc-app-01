# Integration Test Configuration — 本地 MySQL + Redis
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/awsome_shop_auth?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Il0veaws
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      password:
  flyway:
    enabled: true
    baseline-on-migrate: true

security:
  jwt:
    secret: integration-test-secret-key-must-be-at-least-256-bits-long-for-hmac
    expiration: 7200
    issuer: auth-test
  login:
    max-failed-attempts: 5
    lock-duration: 1800

shop:
  remote:
    point:
      base-url: http://localhost:19999
