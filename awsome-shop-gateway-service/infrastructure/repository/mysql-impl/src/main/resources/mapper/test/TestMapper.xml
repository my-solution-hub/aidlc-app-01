<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.awsome.shop.gateway.repository.mysql.mapper.test.TestMapper">

    <!-- 分页查询 -->
    <select id="selectPage" resultType="com.awsome.shop.gateway.repository.mysql.po.test.TestPO">
        SELECT id, name, description, created_at, updated_at, created_by, updated_by, deleted, version
        FROM test
        WHERE deleted = 0
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        ORDER BY created_at DESC
    </select>

</mapper>
