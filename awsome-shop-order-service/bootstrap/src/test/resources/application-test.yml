# Integration Test Configuration — 使用本地 MySQL + Redis
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/awsome_shop_order?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD:Il0veaws}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      password:
  flyway:
    enabled: true
    baseline-on-migrate: true

# 禁用远程服务依赖
shop:
  remote:
    product:
      base-url: http://localhost:19999
    point:
      base-url: http://localhost:19999
