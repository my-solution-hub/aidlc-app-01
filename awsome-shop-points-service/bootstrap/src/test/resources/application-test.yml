# Integration Test Configuration — 使用本地 MySQL + Redis
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/awsome_shop_points?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD:Il0veaws}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      password:
  flyway:
    enabled: true
    baseline-on-migrate: true

# 禁用积分自动发放调度
points:
  distribution:
    cron: "-"
