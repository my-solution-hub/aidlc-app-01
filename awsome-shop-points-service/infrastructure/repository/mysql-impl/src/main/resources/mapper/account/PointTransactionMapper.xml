<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.awsome.shop.point.repository.mysql.mapper.account.PointTransactionMapper">

    <select id="selectPageByUser" resultType="com.awsome.shop.point.repository.mysql.po.account.PointTransactionPO">
        SELECT id, user_id, type, amount, balance, description, created_at
        FROM point_transaction
        WHERE user_id = #{userId}
        <if test="type != null and type != ''">
            AND type = #{type}
        </if>
        ORDER BY created_at DESC
    </select>

    <select id="statDistribution" resultType="com.awsome.shop.point.repository.mysql.po.account.PointGrantStatsPO">
        SELECT
            COALESCE(SUM(amount), 0)        AS grantedTotal,
            COUNT(DISTINCT user_id)         AS coveredEmployees,
            MAX(created_at)                 AS lastGrantedAt
        FROM point_transaction
        WHERE type = 'DISTRIBUTION'
          AND created_at &gt;= #{start}
          AND created_at &lt; #{end}
    </select>

</mapper>
