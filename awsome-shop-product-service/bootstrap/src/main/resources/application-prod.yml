# Production Environment Configuration (EKS)
# 生产环境配置 - Product Service

server:
  port: 8002

spring:
  data:
    redis:
      database: ${REDIS_DATABASE:0}
      password: ${REDIS_PASSWORD:}
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
          max-wait: 5000ms

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 10
      min-idle: 5
      max-active: 50
      max-wait: 60000
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      validation-query: SELECT 1
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000

logging:
  level:
    root: INFO
    com.awsome: INFO

security:
  jwt:
    secret: ${JWT_SECRET:awsomeshop-prod-jwt-secret-change-me-must-be-at-least-256-bits-long-for-hmac-sha256}
    expiration: ${JWT_EXPIRATION:7200}
    issuer: ${JWT_ISSUER:awsome-shop-product-service}
  login:
    max-failed-attempts: ${MAX_FAILED_ATTEMPTS:5}
    lock-duration: ${LOCK_DURATION:1800}

shop:
  security:
    encryption:
      key: ${ENCRYPTION_KEY:awsomeshop32byteEncryptionKeyAa01}
