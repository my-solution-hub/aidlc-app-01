<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.awsome.shop.product.repository.mysql.mapper.category.CategoryMapper">

    <!-- 查询类目列表 -->
    <select id="selectList" resultType="com.awsome.shop.product.repository.mysql.po.category.CategoryPO">
        SELECT id, name, parent_id, icon, sort_order, status, description,
               created_at, updated_at, created_by, updated_by, deleted, version
        FROM category
        WHERE deleted = 0
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY sort_order DESC, id ASC
    </select>

</mapper>
