# 前端对接文档 — 后端新增 20 项能力（v1.2）

> 网关入口：本地 `http://localhost:8088`，线上 `https://d1s51e7b1fz7jr.cloudfront.net`
> 设计稿：`awsome-shop-plan/doc/awsome-shop.pen`
> 所有路径需带服务前缀：auth→`/auth`、product→`/product`、point→`/point`、order→`/order`
> 鉴权：除标注 PUBLIC 外，请求头带 `Authorization: Bearer <token>`；`/admin/**` 需 ADMIN 角色

## ⚠️ 响应信封注意
两种成功判定都要兼容（不同服务实现略有差异）：
- auth / product / point：`{"code":"SUCCESS", "data":...}`
- order：`{"code":0, "success":true, "data":...}`
- **统一判定成功：`res.success === true || res.code === "SUCCESS" || res.code === 0`**

---

## 1. 用户部门字段（A2）

- **.pen 页面**：`Admin - User Management`（用户管理）
- **功能**：用户列表表格的「部门」列；编辑用户时可设置部门
- **后端变化**：`UserDTO` 新增 `department` 字段（无需新接口）
- **前端改动**：
  - 列表 `GET /auth/api/admin/users` 返回的每条记录现含 `department`，直接渲染「部门」列
  - 编辑用户 `PUT /auth/api/admin/users/{id}` 请求体可带 `department`：
    ```json
    {"userId":1, "nickname":"张明辉", "role":"EMPLOYEE", "employeeId":"EMP001", "department":"技术研发部"}
    ```

## 2. 用户统计卡片（A3）🆕接口

- **.pen 页面**：`Admin - User Management` 顶部三张统计卡
- **功能**：总用户数 / 活跃用户 / 本月新增
- **API**：`GET /auth/api/admin/users/stats`（ADMIN）
- **响应**：
  ```json
  {"code":"SUCCESS","data":{"totalUsers":8,"activeUsers":8,"newThisMonth":8}}
  ```

## 3. 商品统计（D1，Dashboard 数据源）🆕接口

- **.pen 页面**：`Admin - Dashboard`（仪表盘）4 张统计卡
- **功能**：Dashboard 需要 4 个数字，分别调 4 个 stats 接口聚合：
  | 卡片 | 接口 |
  |---|---|
  | 总商品数 | `GET /product/api/admin/products/stats` → `{totalProducts, onSaleProducts}` 🆕 |
  | 注册用户 | `GET /auth/api/admin/users/stats` → `totalUsers` |
  | 本月兑换 | `GET /order/api/admin/orders/stats` → 兑换统计 |
  | 积分流通 | `GET /point/api/admin/points/config/stats?month=YYYY-MM` |

## 4. 商品多图（B1）

- **.pen 页面**：`Dialog - 上传图片`、`Employee/Admin - Product Detail`
- **功能**：商品支持多图（最多10张，第一张为主图，可拖拽排序）
- **后端变化**：`ProductDTO` / 创建/更新请求新增 `images: string[]`
- **前端改动**：
  - 创建/编辑商品 `POST/PUT /product/api/admin/products` 请求体带：
    ```json
    {"name":"...","sku":"...","category":"...","pointsPrice":100,"stock":50,"status":1,
     "images":["http://.../1.jpg","http://.../2.jpg"]}
    ```
  - 商品详情 `GET /product/api/products/{id}` 返回 `images` 数组，第一张作主图轮播
  - 图片上传仍走 `POST /product/api/files/upload`（multipart，字段名 `file`），返回 `{filename,url}`，把 url 收集进 `images` 数组

## 5. 同类推荐（D3）🆕接口

- **.pen 页面**：`Employee/Android - Product Detail` 底部「同类推荐」
- **功能**：展示同分类的其他商品（最多6个）
- **API**：`GET /product/api/products/{id}/related`（PUBLIC）
- **响应**：`data` 为 `ProductDTO[]`（已排除当前商品）

## 6. 商品评价（C5）🆕接口

- **.pen 页面**：`Shop Home` / `Product Detail` 的评分「4.5 (128)」、评价区
- **功能**：查看/提交商品评价（评分1-5 + 文字）
- **API**：
  | 操作 | 接口 |
  |---|---|
  | 评价列表 | `GET /product/api/products/{id}/reviews`（PUBLIC）→ `ReviewDTO[]` |
  | 提交评价 | `POST /product/api/products/{id}/reviews`（登录） |
- **提交请求体**：
  ```json
  {"productId":6, "userId":1, "rating":5, "content":"好评"}
  ```
- **ReviewDTO**：`{id, productId, userId, rating, content, createdAt}`

## 7. 心愿单（C6）🆕接口

- **.pen 页面**：`Product Detail`「加入心愿单」按钮
- **功能**：加入/移出/查看心愿单
- **API**（query 参数传 userId、productId）：
  | 操作 | 接口 |
  |---|---|
  | 我的心愿单 | `GET /product/api/wishlist?userId={uid}` → `ProductDTO[]` |
  | 加入 | `POST /product/api/wishlist?userId={uid}&productId={pid}` |
  | 移出 | `DELETE /product/api/wishlist?userId={uid}&productId={pid}` |

## 8. 库存调整（B6）🆕接口

- **.pen 页面**：`Dialog - 调整库存`（入库/出库 + 数量 + 原因）
- **功能**：管理员手动入库/出库，并记录日志
- **API**：`POST /product/api/admin/products/{id}/stock`（ADMIN，query 参数）
  - `changeType`：`IN`（入库）/ `OUT`（出库）
  - `quantity`：调整数量
  - `reason`：调整原因（可选）
  - 示例：`POST /product/api/admin/products/6/stock?changeType=IN&quantity=50&reason=采购到货`
  - 响应：返回更新后的 `ProductDTO`（含新 `stock`）

## 9. 积分规则扩展字段（A4）

- **.pen 页面**：`Dialog - 新增规则` / `编辑规则`
- **功能**：规则表单的「适用范围」「发放方式」「规则图标」
- **后端变化**：`PointRuleDTO` / 创建/更新请求新增 `scope` / `grantMethod` / `icon`
- **前端改动**：创建/更新 `POST/PUT /point/api/admin/point-rules` 请求体带：
  ```json
  {"name":"每月基础积分","ruleType":"固定发放","pointValueMin":500,"pointValueMax":500,
   "triggerCondition":"每月1日自动发放","status":1,
   "scope":"全部员工","grantMethod":"自动发放","icon":"calendar_month"}
  ```

## 10. 积分变动记录操作人（B5）

- **.pen 页面**：`Admin - User Points History`（积分变动记录）「操作人」列
- **后端变化**：`PointTransactionDTO` 新增 `operator`（默认"系统"，管理员调整显示对应人）
- **前端改动**：`GET /point/api/points/transactions?userId={uid}` 返回记录含 `operator`，渲染「操作人」列
  - 其余列已有：`createdAt`(变动时间)、`type`(类型)、`amount`(变动积分)、`balance`(变动后余额)、`description`(原因)

## 11. 收货地址簿（C1）🆕接口

- **.pen 页面**：`Employee/Android - Delivery Info`（填写收货信息）
- **功能**：已保存地址列表、新增/编辑/删除、设默认
- **API**（order 服务，注意 `code:0` 为成功）：
  | 操作 | 接口 |
  |---|---|
  | 地址列表 | `GET /order/api/addresses?userId={uid}` → `AddressDTO[]`（默认地址排最前） |
  | 新增 | `POST /order/api/addresses` |
  | 更新 | `PUT /order/api/addresses/{id}` |
  | 删除 | `DELETE /order/api/addresses/{id}` |
- **请求体（新增/更新）**：
  ```json
  {"userId":1, "receiver":"李四", "phone":"13900139000",
   "region":"北京市海淀区", "detail":"中关村1号", "postalCode":"100000", "isDefault":1}
  ```
- **AddressDTO**：`{id, userId, receiver, phone, region, detail, postalCode, isDefault, createdAt}`
- 注：`isDefault=1` 时后端自动取消该用户其他默认地址

## 12. 下单携带收货地址（C2）

- **.pen 页面**：`Confirm Redemption`（确认兑换）的收货信息区
- **后端变化**：下单请求 `ExchangeRequest` 新增 `addressId`
- **前端改动**：下单 `POST /order/api/orders` 请求体带 `addressId`，后端自动把地址快照存入订单：
  ```json
  {"productId":6, "quantity":1, "userId":1, "employeeName":"李明", "addressId":3}
  ```
  - 订单详情会返回快照字段 `receiver` / `receiverPhone` / `receiverAddress`

## 13. 兑换记录扩展字段（B2 运费/余额 + B4 快递）

- **.pen 页面**：`Exchange Detail`（兑换详情）、`Employee - Order Detail`
- **后端变化**：`ExchangeRecordDTO` 新增：
  - `freightPoints`（运费积分，当前固定0）
  - `balanceAfter`（兑换后余额）
  - `carrier`（快递公司）
  - `receiver` / `receiverPhone` / `receiverAddress`（收货快照）
  - `timeline`（状态时间线，**仅详情接口返回**）
- **前端改动**：详情页直接渲染这些字段，无需新接口

## 14. 订单状态时间线（B3）

- **.pen 页面**：`Employee - Order Detail` 的状态进度条（订单提交→处理中→已发货→已完成）
- **后端变化**：`GET /order/api/orders/{id}` 和 `GET /order/api/admin/orders/{id}` 返回的 DTO 含 `timeline`
- **timeline 结构**：`StatusLogDTO[]`，每项 `{status, remark, time}`，按时间升序
  ```json
  "timeline":[
    {"status":"PENDING_DELIVERY","remark":"订单提交成功，积分已扣除","time":"2026-06-11T11:00:00"},
    {"status":"DELIVERING","remark":"状态更新为 DELIVERING","time":"2026-06-11T11:05:00"},
    {"status":"COMPLETED","remark":"员工已确认收货","time":"2026-06-11T11:10:00"}
  ]
  ```

## 15. 管理员发货带快递公司（B4）

- **.pen 页面**：`Dialog - 修改发货状态`（目标状态 + 快递公司 + 快递单号 + 备注）
- **后端变化**：`PUT /order/api/admin/orders/{id}/status` 请求体新增 `carrier`
- **请求体**：
  ```json
  {"id":6, "status":"DELIVERING", "trackingNumber":"SF1234567890", "carrier":"顺丰速运"}
  ```
  - 合法状态流转：`PENDING_DELIVERY → DELIVERING → COMPLETED`，或任意 → `CANCELLED`

## 16. 员工确认收货（C3）🆕接口

- **.pen 页面**：`Order Detail` / `Redemption History` 的「确认收货」按钮
- **功能**：员工把已发货订单确认为完成（DELIVERING→COMPLETED）
- **API**：`POST /order/api/orders/{id}/confirm-receipt?userId={uid}`（登录）
  - `userId` 用于校验订单归属（仅本人可确认）
  - 响应：返回更新后 `ExchangeRecordDTO`（status=COMPLETED）

## 17. 员工订单关键词搜索（A1）

- **.pen 页面**：`Employee - Redemption History`（兑换记录）的「搜索订单编号或商品名称」框
- **后端变化**：`GET /order/api/orders` 新增 `keyword` 查询参数
- **前端改动**：
  ```
  GET /order/api/orders?userId=1&page=1&size=10&keyword=耳机
  ```
  - keyword 模糊匹配订单号或商品名；中文记得 URL 编码

## 18. 取消订单退款（C4）

- **.pen 页面**：`Exchange Detail`「取消订单」
- **功能**：后端**已支持**，取消时自动退还积分 + 恢复库存
- **前端改动**：`PUT /order/api/admin/orders/{id}/status` 传 `{"id":6,"status":"CANCELLED"}` 即可

## 19. 多级分类（D4）

- **.pen 页面**：`Category Management`「添加子类」
- **功能**：后端**已支持**父子分类
- **前端改动**：
  - 建子类：`POST /product/api/admin/categories` 请求体带 `parentId`
  - `GET /product/api/categories/tree` 返回的一级分类含 `children` 子分类数组

## 20. 数据导出 CSV（D2）🆕接口

- **.pen 页面**：`User Management`「导出数据」、`Exchange Records`「导出记录」
- **功能**：导出 CSV 文件（UTF-8 含 BOM，可直接 Excel 打开）
- **API**：
  | 导出 | 接口 |
  |---|---|
  | 用户 | `GET /auth/api/admin/users/export` |
  | 兑换记录 | `GET /order/api/admin/orders/export` |
- **前端处理**：这两个返回的是 `text/csv`（非 JSON），用 `responseType:'blob'` 下载：
  ```js
  const res = await axios.get('/auth/api/admin/users/export', {responseType:'blob'});
  const url = URL.createObjectURL(res.data);
  const a = document.createElement('a'); a.href=url; a.download='users.csv'; a.click();
  ```

---

## 附：完整新接口一览（13 个新端点）

| # | 方法 | 路径（含网关前缀） | 鉴权 |
|---|---|---|---|
| 1 | GET | `/auth/api/admin/users/stats` | ADMIN |
| 2 | GET | `/auth/api/admin/users/export` | ADMIN |
| 3 | GET | `/product/api/admin/products/stats` | ADMIN |
| 4 | POST | `/product/api/admin/products/{id}/stock` | ADMIN |
| 5 | GET | `/product/api/products/{id}/related` | PUBLIC |
| 6 | GET | `/product/api/products/{id}/reviews` | PUBLIC |
| 7 | POST | `/product/api/products/{id}/reviews` | 登录 |
| 8 | GET | `/product/api/wishlist` | 登录 |
| 9 | POST | `/product/api/wishlist` | 登录 |
| 10 | DELETE | `/product/api/wishlist` | 登录 |
| 11 | GET | `/order/api/addresses` | 登录 |
| 12 | POST | `/order/api/addresses` | 登录 |
| 13 | PUT | `/order/api/addresses/{id}` | 登录 |
| 14 | DELETE | `/order/api/addresses/{id}` | 登录 |
| 15 | POST | `/order/api/orders/{id}/confirm-receipt` | 登录 |
| 16 | GET | `/order/api/admin/orders/export` | ADMIN |

> 完整字段定义见 `api-docs/awsomeshop-openapi.yaml`（53路径/65接口/89schema），可导入 Postman/Apifox。
> 在线 Swagger：`http://localhost:8088/swagger-ui.html`
