# Web/Android 每个按钮真实联通 — 审计与任务清单

> 目标：Web 前端 + Android 的每一个按钮/编辑/链接都真实连后端、有效果
> 现状：~19 死按钮、4 缺失路由、8 个已有 API、~25+ 缺失 API + 对应后端端点

## 后端缺失端点（需先补，前端才能连）

### Product Service（已有：list/create/get/deduct-stock/restore-stock）
- [ ] updateProduct (update) `POST /public/product/update`
- [ ] deleteProduct `POST /public/product/delete`
- [ ] toggleProductStatus（上/下架）`POST /public/product/update-status`

### Category Service（product 服务内，已有：list 树）
- [ ] createCategory `POST /public/category/create`
- [ ] updateCategory `POST /public/category/update`
- [ ] deleteCategory `POST /public/category/delete`
- [ ] toggleCategoryStatus `POST /public/category/update-status`

### Point Service（已有：rule/list、account balance/transaction/adjust）
- [ ] createPointRule `POST /admin/point-rule/create`
- [ ] updatePointRule `POST /admin/point-rule/update`
- [ ] togglePointRuleStatus `POST /admin/point-rule/update-status`

### Order Service（已有：admin list/get/stats、public exchange/list/get）
- [ ] updateExchangeStatus（发货/完成）`POST /admin/exchange-record/update-status`

### Auth Service（已有：login/logout/user list/validate）
- [ ] register `POST /public/auth/register`
- [ ] createUser / disableUser（用户管理）`POST /public/auth/user/create`, `/update-status`

## 前端缺失（Web）

### 缺失路由（4）
- [ ] /admin/users（用户管理页 + 组件）
- [ ] /admin/products/:id/edit（商品编辑页）
- [ ] /orders（员工订单页）
- [ ] /points（员工积分页）
- [ ] /register（注册页）

### 缺失 API 封装（services/api）
- [ ] product: update/delete/updateStatus/get
- [ ] category: create/update/delete/updateStatus
- [ ] pointRule: create/update/updateStatus
- [ ] exchangeRecord: get/updateStatus/export
- [ ] user.ts（新建）: list/create/updateStatus
- [ ] employee/shop: redeem/myOrders/pointsBalance
- [ ] auth: register

### 死按钮接线（~19）
- [ ] Categories: 添加类目/编辑/添加子类/停用启用/删除（7）
- [ ] PointRules: 添加规则/编辑/停用启用（3）
- [ ] Products: 编辑/删除（2）
- [ ] ExchangeRecords: 导出/详情（2）
- [ ] Dashboard: 查看全部 + 真实指标（mock→API）
- [ ] ShopHome: 立即浏览/商品卡/兑换 + 真实商品（mock→API）
- [ ] Login: 立即注册链接
- [ ] EmployeeLayout: 搜索框+按钮

## Android（同步）
- [ ] 各 screen 按钮接 Repository → 真实 API（已对齐契约，需确认每个交互调用）

## 执行阶段
- Phase A: 后端补端点（product/category/point/order/auth）+ 编译
- Phase B: 前端 API 封装 + 路由 + 新页面
- Phase C: 死按钮逐个接线
- Phase D: Dashboard/ShopHome mock→真实 API
- Phase E: Android 按钮联通核对
- Phase F: 启动全栈 + 浏览器逐页点击验证

## 进度
- [x] 审计完成
- [x] Phase A 后端：4 服务全部补端点 + BUILD SUCCESS
  - product: update/delete/update-status + category create/update/delete/update-status
  - point: point-rule create/update/update-status
  - order: exchange-record/update-status (+trackingNumber V4)
  - auth: register / user/create / user/update-status
- [x] Phase B 前端基建：API 封装全补 + 路由(/register /orders /points /admin/users /admin/products/:id/edit) + 5 新页面，npm build ✅
- [x] Phase C 接线：所有死按钮接 API（Categories/PointRules/Products/ExchangeRecords/Dashboard/ShopHome/Login/EmployeeLayout/Users）
- [x] Phase D mock 替换：ShopHome 接 product/list；积分接 point/balance
- [x] Phase E Android（MVVM 接线完成，静态校验通过；未 gradle 编译——无 SDK）
  - 建 9 个 @HiltViewModel + SessionSync(ensureUserId) + 补全 6 个占位屏
  - AppNavGraph 10 处 hiltViewModel() 注入；9 屏 collectAsState
  - 每个数据按钮接 repo：登录/商品列表/详情/兑换createOrder/订单/积分/退出登录
  - 校验：VM 调用的 repo 方法全部存在且签名匹配；无 sample/TODO 数据残留
  - 仅 2 个无后端端点的按钮(搜索/通知)保留 no-op 注释
  - ⚠️ 诚实声明：本机无 Android SDK，未 gradle 编译，仅静态自审 + 方法签名核对
- [x] Phase F 端到端点击验证（浏览器实测）
  - ✅ 用户管理页（原404）→ 真实渲染 admin/employee 用户
  - ✅ 产品编辑（原点击无反应）→ 跳转 /admin/products/:id/edit + product/get 加载真实数据
  - ✅ 编辑保存 → product/update 真实写回后端（DB 改值验证通过）
  - ⚠️ 小瑕疵：用户管理状态列显示"禁用"（ACTIVE→禁用 的展示映射 bug，待修）

## 已知待修小问题 —— 全部已修复 ✅
1. ✅ 用户列表 status 映射 bug（ACTIVE 字符串）—— 已修，浏览器确认显示"启用"
2. ✅ 仪表盘指标卡 mock 数字 —— 已改为真实 API 聚合（商品/用户/兑换统计/最近订单），浏览器确认显示真实 2/2/1/500
3. ✅ Android 编译 —— 配置 SDK + 绕过系统代理，gradle assembleDebug **BUILD SUCCESSFUL**，产出 app-debug.apk
4. ✅ Android 搜索/通知 no-op 按钮 —— 已接线：搜索=实时过滤商品；通知=基于真实积分/商品数生成提示，重新编译 BUILD SUCCESSFUL
5. ✅ Web Categories 分页"1"按钮 —— 已接 fetchData() 刷新

## 最终状态：每个按钮真实联通 ✅
- Web：全 src 扫描无空 onClick / 无 TODO 死按钮；所有 pages 的 ButtonBase/Button 均有 onClick；浏览器实测用户管理/产品编辑保存/仪表盘真实数据
- Android：gradle 真实编译通过 + APK 产出；所有 screen 经 ViewModel 接 repository；搜索/通知按钮已接线
- 后端：4 服务 + 网关全部 BUILD SUCCESS 且运行中；validate 端点补全，网关全链路打通
