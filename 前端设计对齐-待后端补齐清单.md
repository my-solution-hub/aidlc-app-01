# 前端设计稿对齐 · 待后端补齐的 REST 端点 / 字段需求

> 作者：前端 lane（frontend/design-alignment 分支）
> 日期：2026-06-10
> 背景：本轮前端按 `awsome-shop-plan/doc/awsome-shop.pen` 设计稿对齐，**纯前端能做的已全部实现**（详见 PR 描述）。
> 本文档列出**前端已搭好 UI、但缺后端端点/字段**的项，供后端 lane 对齐。
> 契约风格：沿用当前 REST（`API接口文档.md`）：`/api/...`(PUBLIC/AUTHENTICATED)、`/api/admin/...`(ADMIN)、`/api/internal/...`；统一 `Result<T>` 信封。
> 与 `Olivia-AWSomeShop-後端API缺口與規格.md` 的关系：标注 ♻️ 的项 Olivia 已规格过（此处更新为 REST 路径并复述）；标注 🆕 的是本轮设计对齐**新发现、Olivia 文档未覆盖**的缺口。

---

## 优先级总览

| # | 缺口 | 涉及设计页 | 优先级 | 来源 |
|---|---|---|---|---|
| 1 | 管理端·员工积分列表 | adm-03/adm-10 | 🔴 P0 | ♻️ |
| 2 | 管理端·积分变动明细（按用户） | adm-10 | 🔴 P0 | ♻️ |
| 3 | 管理端·手动调整积分 | dlg-10 | 🔴 P0 | ♻️ |
| 4 | 用户·部门字段 + 用户统计 | adm-03 | 🟡 P1 | 🆕 |
| 5 | 员工·确认收货端点 | emp-07/emp-08 | 🟡 P1 | 🆕 |
| 6 | 收货地址簿 + 订单收货信息 | emp-05/emp-07/adm-09 | 🟡 P1 | 🆕 |
| 7 | 兑换记录 DTO 扩展字段 | adm-09 | 🟡 P1 | 🆕 |
| 8 | 积分规则 DTO 扩展字段 | dlg-07/dlg-08 | 🟢 P2 | 🆕 |
| 9 | 积分发放统计 | adm-07 | 🟢 P2 | ♻️ |
| 10 | 员工订单列表关键字搜索 | emp-08 | 🟢 P2 | 🆕 |
| 11 | 商品多图（图廊） | adm-04/emp-03 | 🟢 P2 | 🆕 |

> 当前前端对这些缺口的处理：相关字段一律显示 `—`，相关动作按钮隐藏或禁用，**不渲染假数据**。后端补齐后前端只需接线。

---

## 🔴 1~3. 管理端积分管理（员工积分列表 / 明细 / 手动调整）

前端页面 **adm-10 用户积分历史**、弹窗 **dlg-10 调整用户积分** 已设计，但缺后端。前端 service 计划新增 `services/api/userPoint.ts`。

### 1. 员工积分列表 `GET /api/admin/points/users?page=&size=&keyword=`
- 鉴权：ADMIN
- 响应 `PageResult<UserPointDTO>`
- `UserPointDTO`: `{ userId, username, nickname, employeeId, department, balance, totalEarned, totalUsed, exchangeCount, updatedAt }`
  - 注：`balance/totalEarned/totalUsed` 同 `PointBalanceDTO`；`exchangeCount` = 该用户兑换订单数。
  - 用途：**adm-03 用户管理的「积分余额 / 兑换次数」列**也依赖本接口或在 `/api/admin/users` 里补这两个字段（二选一，建议合并进用户列表更省）。

### 2. 用户积分变动明细 `GET /api/admin/points/users/{userId}/transactions?page=&size=&type=`
- 鉴权：ADMIN
- 响应 `PageResult<PointTransactionDTO>`（沿用既有 DTO，`type` ∈ `EARN/REDEEM/ADJUST/INIT/DISTRIBUTION`）
- 替代方案：若允许 ADMIN 调用现有 `GET /api/points/transactions?userId=`，可不新增（但建议走 admin 路径做权限隔离）。

### 3. 手动调整积分 `POST /api/admin/points/users/{userId}/adjust`
- 鉴权：ADMIN
- 请求：`{ amount: number, reason: string }`（amount 正=增、负=减；reason 必填）
- 响应：`PointBalanceDTO`（调整后）
- 规则：扣减后余额 < 0 → `CONFLICT_POINT_BALANCE`；同时写一条 `PointTransaction(type=ADJUST, description=reason)`。
- 注：现有 `POST /api/internal/points/adjust` 是 internal（不经网关），需要一个 admin 对外变体。

---

## 🟡 4. 用户·部门字段 + 用户统计（adm-03）

设计稿用户列表有「部门」列、顶部有「活跃用户 / 本月新增」统计卡。

- **字段**：`UserDTO` 增加 `department?: string`（前端类型已预留位，列已留位显示 `—`）。
  - 连带：`/api/admin/users` 创建/更新支持 `department`；建议 `/api/auth/register` 也可带。
- **统计** `GET /api/admin/users/stats` → `{ totalUsers, activeUsers, newThisMonth }`
  - 前端现状：总用户数用列表 `total` 显示真实值；活跃/本月新增显示 `—`。

---

## 🟡 5. 员工·确认收货（emp-07 订单详情 / emp-08 兑换记录）

设计稿员工端有「确认收货」按钮（待发货/已发货 → 已完成）。现有只有管理员 `PUT /api/admin/orders/{id}/status`。

- 建议 `POST /api/orders/{id}/confirm-receipt`
  - 鉴权：AUTHENTICATED（且校验订单属于当前用户）
  - 行为：`DELIVERING → COMPLETED`
  - 响应：`ExchangeRecordDTO`
- 前端现状：员工端「确认收货」按钮暂未渲染，待此端点补齐后接线。

---

## 🟡 6. 收货地址簿 + 订单收货信息（emp-05 填写收货信息 / emp-07 / adm-09）

设计稿有独立「填写收货信息」页（已保存地址 + 新增地址表单），且订单详情/兑换详情展示收货人/电话/地址。当前 `ExchangeRecordDTO` 无任何地址字段，也无地址簿。

- **地址簿**（员工，AUTHENTICATED）：
  - `GET /api/addresses?userId=` → `AddressDTO[]`
  - `POST /api/addresses` 新增；`PUT /api/addresses/{id}`；`DELETE /api/addresses/{id}`；`PATCH /api/addresses/{id}/default`
  - `AddressDTO`: `{ id, userId, recipient, phone, province, city, district, detail, zipcode?, isDefault }`
- **下单携带地址**：`POST /api/orders` 请求体增加 `addressId`（或内联收货信息），并落库到兑换记录。
- **兑换记录展示**：`ExchangeRecordDTO` 增加 `recipient, phone, address`（见下条 7）。
- 前端现状：兑换流程暂为 **详情 → 确认 → 成功**（跳过地址页）；订单/兑换详情收货人用 `employeeName` 占位，其余 `—`。

---

## 🟡 7. 兑换记录 DTO 扩展字段（adm-09 兑换详情）

设计稿兑换详情展示更多字段，现有 `ExchangeRecordDTO` 缺：

| 字段 | 说明 |
|---|---|
| `shippingPoints` | 运费积分（设计有「运费积分」行，目前恒 0） |
| `balanceAfter` | 兑换后余额 |
| `employeeNo` | 兑换员工工号 |
| `department` | 兑换员工部门 |
| `contact` | 联系方式（邮箱/电话） |
| `recipient` / `phone` / `address` | 收货信息（见第 6 条） |
| `courierCompany` | 快递公司（dlg-09 修改发货状态表单字段） |
| `remark` | 备注（dlg-09 字段 + 订单信息「备注」行） |
| `source` | 订单来源（PC端/网页版等） |
| `statusHistory[]` | 状态记录时间线（`{status, time, note}[]`，设计右侧「状态记录」时间线） |

- 前端现状：dlg-09 仅提交 `status + trackingNumber`（后端已支持）；`courierCompany/remark` 等字段暂未加入表单，待 DTO/端点扩展后补。详情页缺失字段显示 `—`。

---

## 🟢 8. 积分规则 DTO 扩展字段（dlg-07 / dlg-08 新增/编辑规则）

设计稿规则弹窗有「适用范围 / 发放方式 / 规则图标」，现有 `PointRuleDTO` 无：

- `applicableScope?: string`（如 全部员工/指定部门）
- `grantMethod?: string`（自动发放/手动发放）
- `icon?: string`
- 前端现状：规则弹窗暂为现有字段（名称/类型/积分区间/触发条件/描述），上述三项待后端加字段后补表单项。

---

## 🟢 9. 积分发放统计（adm-07 积分规则页统计卡）

设计稿统计卡「本月发放 / 覆盖员工」，现有 `point-config` 仅 `{amount}`，无统计。

- `GET /api/admin/points/config/stats?month=YYYY-MM` → `{ month, grantedTotal, coveredEmployees, lastGrantedAt }`
- 前端现状：统计卡「本月发放 / 覆盖员工」显示 `—`。

---

## 🟢 10. 员工订单列表关键字搜索（emp-08）

设计稿兑换记录有搜索框。现有 `GET /api/orders` 无 `keyword` 参数。

- 建议 `GET /api/orders?...&keyword=`（按订单号/商品名）
- 前端现状：搜索改为**当前页客户端过滤**（降级），后端加 `keyword` 后切回服务端。

---

## 🟢 11. 商品多图 / 图廊（adm-04 商品详情 / emp-03 商品详情）

设计稿商品有多图 + 缩略图廊 + 主图标记，现有 `ProductDTO` 只有单 `imageUrl`。

- 建议 `ProductDTO` 增加 `images?: string[]`（首张为主图），创建/更新支持。
- 前端现状：dlg-03 上传图片只设单张主图（`imageUrl`），多图廊待 `images[]` 后扩展。

---

## 附：前端本轮已用的现有端点（无需改动）

`GET /api/products`、`GET /api/products/{id}`、`PUT /api/admin/products/{id}`、`PATCH /api/admin/products/{id}/status`、`POST /api/files/upload`、`GET /api/categories/tree`、`GET /api/orders`、`GET /api/orders/{id}`、`POST /api/orders`、`GET /api/admin/orders`、`GET /api/admin/orders/{id}`、`PUT /api/admin/orders/{id}/status`、`GET /api/points/balance`、`GET /api/points/transactions`、`GET /api/admin/users`、`POST /api/admin/users`、`PUT /api/admin/users/{id}`、`PATCH /api/admin/users/{id}/status`。
