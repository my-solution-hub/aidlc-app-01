# 前端设计对齐 · 对后端的依赖（接口/字段需求）

> 作者：前端 lane（`frontend/pen-design-alignment`）
> 更新：2026-06-10，**已对齐最新 main**（含 `d60274e 补齐积分管理缺口`、`bfb0a0e API文档v1.1`）
> 目的：明确「前端按 `awsome-shop-plan/doc/awsome-shop.pen` 设计稿对齐时，**仍缺的后端能力**」，供后端同事直接开工。
> 契约风格：沿用当前 REST（见 `API接口文档.md`）：`/api/...`(PUBLIC/AUTHENTICATED)、`/api/admin/...`(ADMIN)、统一 `Result<T>` 信封、错误码前缀决定 HTTP 状态。
> 处理原则：缺字段前端显示 `—`、缺端点对应按钮隐藏/禁用，**不造假数据**；后端补齐后前端接线即可。

---

## ✅ 已就绪（本次确认无需后端再做）
最新 main 已提供，前端将自行接线（属前端待办，非后端依赖）：
- 管理端员工积分列表 `GET /api/admin/points/users`
- 手动调整积分 `POST /api/admin/points/adjust`
- 积分发放统计 `GET /api/admin/points/config/stats`
- 发放配置 `GET/PUT /api/admin/points/config`（含 cycle/grantDay/enabled）
- 用户 `employeeId`、用户更新 `PUT /api/admin/users/{id}`、文件上传 `POST /api/files/upload`、注册带 `employeeId`

---

## 🔴 仍缺的后端能力（按优先级）

### 1. 用户「部门」字段 + 用户统计（adm-03 用户管理）
设计稿用户列表有「**部门**」列，顶部有「**活跃用户 / 本月新增**」统计卡。

- **字段**：`UserDTO` 增加 `department`（string，可空）；`POST/PUT /api/admin/users`、`POST /api/auth/register` 支持写入。
- **统计**：`GET /api/admin/users/stats` → `{ totalUsers, activeUsers, newThisMonth }`（ADMIN）
  - activeUsers 口径建议：status=ACTIVE 计数；newThisMonth：本月 createdAt 计数。
- 现状：部门列、活跃/本月新增统计卡显示 `—`（总用户数已用列表 total 显示真实值）。

### 2. 员工「确认收货」（emp-07 订单详情 / emp-08 兑换记录）
设计稿员工端有「确认收货」按钮（DELIVERING → COMPLETED）。当前仅管理员可改状态（`PUT /api/admin/orders/{id}/status`）。

- 建议 `POST /api/orders/{id}/confirm-receipt`（AUTHENTICATED，校验订单属当前用户）→ `ExchangeRecordDTO`
- 现状：员工端「确认收货」按钮未渲染。

### 3. 收货地址簿 + 订单收货信息（emp-05 填写收货信息 / emp-07 / adm-09）
设计稿有独立「填写收货信息」页（已存地址单选 + 新增地址表单），订单/兑换详情展示收货人/电话/地址。当前完全缺失。

- **地址簿**（AUTHENTICATED）：
  - `GET /api/addresses?userId=` → `AddressDTO[]`
  - `POST /api/addresses` / `PUT /api/addresses/{id}` / `DELETE /api/addresses/{id}` / `PATCH /api/addresses/{id}/default`
  - `AddressDTO`: `{ id, userId, recipient, phone, province, city, district, detail, zipcode?, isDefault }`
- **下单带地址**：`POST /api/orders` body 增加 `addressId`（或内联收货信息），落库到兑换记录。
- 现状：兑换流程为 **商品详情 → 确认兑换 → 兑换成功**（暂跳过地址页）；详情页收货人用 `employeeName` 占位、其余 `—`。

### 4. 兑换记录 DTO 扩展（adm-09 兑换详情 / dlg-09 修改发货状态）
设计稿展示更多字段，现有 `ExchangeRecordDTO` 缺：

| 字段 | 说明 |
|---|---|
| `shippingPoints` | 运费积分（详情「运费积分」行，目前恒 0） |
| `balanceAfter` | 兑换后余额 |
| `employeeNo` / `department` / `contact` | 兑换员工 工号/部门/联系方式 |
| `recipient` / `phone` / `address` | 收货信息（依赖第 3 条） |
| `courierCompany` / `remark` | dlg-09「快递公司 / 备注」表单字段（现仅提交 status + trackingNumber，后端已支持） |
| `source` | 订单来源 |
| `statusHistory[]` | 状态时间线 `{status, time, note}[]`（详情右侧「状态记录」） |

- 现状：dlg-09 仅 `status + trackingNumber`；详情缺字段显示 `—`。

### 5. 积分规则 DTO 扩展（dlg-07 / dlg-08 新增/编辑规则）
设计稿规则弹窗有「适用范围 / 发放方式 / 规则图标」，现有 `PointRuleDTO` 无：
- `applicableScope?`（如 全部员工/指定部门）、`grantMethod?`（自动/手动）、`icon?`
- 现状：规则弹窗仅现有字段（名称/类型/积分区间/触发条件/描述）。

### 6. 员工订单列表关键字搜索（emp-08）
设计稿兑换记录有搜索框。现有 `GET /api/orders` 无 `keyword`（仅管理端 `/api/admin/orders` 有）。
- 建议 `GET /api/orders?...&keyword=`（按订单号/商品名）
- 现状：前端改为**当前页客户端过滤**（降级），后端加 `keyword` 后切回服务端。

### 7. 商品多图 / 图廊（adm-04 / emp-03 商品详情）
设计稿商品有多图 + 缩略图廊 + 主图标记，现有 `ProductDTO` 只有单 `imageUrl`。
- 建议 `ProductDTO` 增加 `images?: string[]`（首张为主图），创建/更新支持。
- 现状：上传图片只设单张主图（`imageUrl`）。

---

## 端点总表（仅"仍缺"项）
| 方法 | 端点 | 说明 |
|---|---|---|
| GET | `/api/admin/users/stats` | 用户统计（总数/活跃/本月新增）|
| — | `UserDTO.department` 字段 | 用户部门 |
| POST | `/api/orders/{id}/confirm-receipt` | 员工确认收货 |
| GET/POST/PUT/DELETE/PATCH | `/api/addresses...` | 收货地址簿 |
| — | `POST /api/orders` 增 `addressId` | 下单带地址 |
| — | `ExchangeRecordDTO` 扩展字段 | 见第 4 节 |
| — | `PointRuleDTO` 扩展字段 | 见第 5 节 |
| GET | `/api/orders?keyword=` | 员工订单搜索 |
| — | `ProductDTO.images[]` | 商品多图 |

---

## 附：前端待开发（后端已就绪，属前端 backlog，非后端依赖）
> 这些后端已具备，待前端接线（前端 lane 后续完成）：
> - adm-10 用户积分变动记录页 + dlg-10 调整积分（`/api/admin/points/adjust` + `/api/points/transactions?userId=`）
> - adm-07 积分规则页统计卡「本月发放/覆盖员工」（`/api/admin/points/config/stats`）
> - adm-03 用户列「积分余额」（`/api/admin/points/users`）
