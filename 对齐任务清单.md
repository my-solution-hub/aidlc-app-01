# 规格对齐任务清单（按 plan 设计文档逐项对齐）

> 目标：实现与 awsome-shop-plan 设计规格完整对齐，每项用 codex 验证一致性。跳过 Android。前端用 MUI MCP 保持一致。
> 验证方式：每项做完 → mvn/npm 编译 → codex 验证是否与规格一致。

## 执行顺序（按依赖 + 价值）

### Phase 1: Infrastructure（Unit 7）✅ 完成
- [x] 1.1 docker-compose.yml（mysql+redis+4后端+网关+前端，网络/卷/healthcheck/depends_on）
- [x] 1.2 6 个 Dockerfile（5 后端多阶段 Maven JDK21 + 前端 node→nginx）
- [x] 1.3 MySQL init（建 5 库；表交 Flyway，与真实代码一致）
- [x] 1.4 .env.example + nginx default.conf
- [x] codex 验证：DB wiring/库/jar路径/网关路由 全对；修复 JWT_SECRET/ENCRYPTION_KEY 缺默认值
- 关键修复：网关路由配置补进 docker profile（否则启动崩溃）；点服务库名单数 awsome_shop_point

### Phase 2: Order Saga 修正 ✅ 完成（codex 验证 4/4 通过）
- [x] 2.1 扣减顺序改「先积分后库存」+ 补偿方向对应（BR-ORDER-003）
- [x] 2.2 状态机流转图校验 STATUS_TRANSITIONS（BR-ORDER-006）
- [x] 2.3 取消自动退积分+恢复库存（BR-ORDER-007）
- [x] 2.4 跨服务 3s 超时 .block(Duration.ofSeconds(3))（BR-ORDER-009）
- [x] mvn BUILD SUCCESS + codex 验证全部正确

### Phase 3: Points Service 对齐 ✅ 核心完成（BUILD SUCCESS）
- [x] 3.1 SystemConfig 实体/表 + V4 迁移 + 种子 points.distribution.amount=100
- [x] 3.3 定时自动发放 @Scheduled(cron 月度) + 手动触发端点 /internal/point/distribute（FR-POINT-001/BR-POINTS-008/009）
- [x] 3.4 admin /point/config/get + /config/update
- [~] 3.2 internal /init /deduct 已等价存在（用 /adjust direction），/rollback 用 refund 等价
- [~] 3.5 TransactionType：新增 DISTRIBUTION，保留现有 EARN/REDEEM/ADJUST/INIT（不破坏 order/auth 依赖）
- 说明：枚举完全改名会破坏已联通的 order Saga + 前端，故采取「新增不破坏」策略

### Phase 4: Auth Service 对齐 ✅ codex IMPLEMENTED
- [x] 4.1 employeeId 字段 + V3 迁移 + 唯一索引 + 种子回填
- [x] 4.2 /auth/me、/user/get、/user/update 端点
- [x] 4.3 注册→PointRemoteClient.initPointAccount（BR-AUTH-007，best-effort）
- [x] mvn BUILD SUCCESS + codex 验证

### Phase 5: Product Service 对齐 ✅ codex IMPLEMENTED
- [x] 5.1 文件上传 /public/file/upload + 5MB/类型校验 + FileErrorCode
- [x] 5.2 库存悲观锁 selectByIdForUpdate（SELECT...FOR UPDATE，BR-PROD-007）
- [x] 5.3 分类创建拒绝 >2 级（CAT_002）
- [x] mvn BUILD SUCCESS + codex 验证

### Phase 6: Gateway RBAC ✅ codex IMPLEMENTED
- [x] 6.1 validate 返回 role + 含 /admin/ 路径要求 ADMIN 否则 403 + 注入 X-User-Role
- [x] auth + gateway 双 BUILD SUCCESS + codex 验证

### Phase 7: 前端对齐（Pencil MCP 检查；.pen 为空占位，按设计规格 + 现有 MUI 风格实现）
- [x] 7.1 产品详情页 /products/:id（ProductDetail，getProduct）
- [x] 7.2 兑换确认页 /orders/confirm/:productId（getProduct+getBalance+redeemProduct）
- [x] ShopHome 卡片/兑换按钮改为跳新页面；npm build ✅ + codex 验证

## 说明
- API 风格（RPC vs REST）系统性偏离：成本极高且会破坏已联通的前后端，**暂不强行改 REST**，保留现有 POST 风格并在文档记录偏离。优先补「功能缺失」而非「风格重构」。
- 每个 Phase 完成后更新本清单 + codex 验证记录。

## 进度
- [x] 对比报告完成（auth/points/product/order/gateway/frontend/infra）
- [ ] Phase 1-7 待执行
